// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Personal {
  uid          String  @id @unique
  name         String
  surname      String
  identityCard String
  typeCard     String
  phone        String
  address      String
  workArea     String
  job          String
  salary       Decimal
  hours        Hour[]
}

model Hour {
  id         Int      @id @default(autoincrement())
  date       DateTime
  startTime  String
  endTime    String
  hours      String
  dayHours   String
  personal   Personal @relation(fields: [personalId], references: [uid])
  personalId String
}
